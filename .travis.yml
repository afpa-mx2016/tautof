language:
    php

php:
    - 7.0

services:
    - mysql

before_install:
    - composer self-update
    - mysql -u root -e 'create database testdbtautof;'

before_script:
  - composer install

script:
  - cd $TRAVIS_BUILD_DIR
  - phpunit

notifications:
  email:
    -  lionel.duboeuf@gmail.com
