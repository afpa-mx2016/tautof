language:
    php

php:
    - 7.0

services:
    - mysql

before_install:
    - composer self-update
    - mysql -u root -e 'create database testdbtautof;'

before_script:
  - composer install

script:
  - phpunit tests

notifications:
  email:
    -  lionel.duboeuf@gmail.com
